prompt for cf-remote (aws), vagrant, or existing host (also args)
prompt for platform (if not existing host)
if existing host, prompt for user@host
spawn/test connection
keep a state file of the current build host for use in later scripts
